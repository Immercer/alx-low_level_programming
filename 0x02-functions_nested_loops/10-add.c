#include <stdio.h>
#include "main.h"
/**
 * add - Add to numbers
 * Return: addition of 2 numbers
 * @x: parameter
 * @y: parameter
 */
int add(int x, int y)
{
	int results;

	results = x + y;
	return (results);
}
